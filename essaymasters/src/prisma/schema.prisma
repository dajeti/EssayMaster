generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://Developer_test:e55aymaster@essaymasterdb.yzhv4.mongodb.net/essaymasterdb?retryWrites=true&w=majority"
}

model Chats {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model MarkScheme {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  sessionId Json?  @unique
}

model Revision {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model Session {
  id         String   @id @default(auto()) @map("_id") @db.ObjectId
  /// Could not determine type: the field only had null or empty values in the sample set.
  chat       Json?
  createdAt  DateTime @db.Date
  essay      String
  feedback   String
  /// Could not determine type: the field only had null or empty values in the sample set.
  markScheme Json?
  updatedAt  DateTime @db.Date
}

model User {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  email     String @unique
  firstName String
  lastName  String
  password  String
}
